1、build was configured to prefer settings repositories over project repositories”解决方法？
    检查检查setting.gradle build.gradle 这两个文件的repositories是否一致。
    删除setting.gradle 中 dependencyResolutionManagement{}一整段代码